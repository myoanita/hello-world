# to use orbs, must use version >= 2.1
version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          yarn: true
          store_artifacts: true
          record: true
          env: --key 445d43ee-5f46-4144-b428-286de6535c1c